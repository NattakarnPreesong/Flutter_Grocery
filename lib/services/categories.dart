import 'package:flutter/material.dart';

class CaregoriesScreen extends StatelessWidget {
  const CaregoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("Caregories Scren"),
    ));
  }
}